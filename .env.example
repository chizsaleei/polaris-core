# =========================
# Polaris Core (Express) on AWS Elastic Beanstalk
# =========================
# Notes:
# - EB expects your app to listen on PORT=8080
# - APP_BASE_URL should be your FRONTEND (Vercel) URL, not localhost
# - NEXT_PUBLIC_* stay in Vercel (frontend) env, not AWS

# ---- Required runtime ----
NODE_ENV=staging
PORT=8080
APP_BASE_URL=https://YOUR-VERCEL-FRONTEND-DOMAIN
ADMIN_API_KEY=
# Optional: protect core with an API key
# CORE_API_KEY=your_long_random_key

# ---- Supabase (server side) ----
SUPABASE_URL=https://YOUR-SUPABASE-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY
# Optional, only if used in auth verification logic
SUPABASE_JWT_SECRET=YOUR_SUPABASE_JWT_SECRET

# ---- CORS ----
CORS_ORIGIN=https://YOUR-VERCEL-FRONTEND-DOMAIN
# Optional extra origins, comma-separated
# CORS_ADDITIONAL_ORIGINS=https://YOUR-VERCEL-PREVIEW-DOMAIN,http://localhost:3000

# ---- Billing providers (required by env.ts) ----
# One of: paypal | paymongo | paypal,paymongo
BILLING_PROVIDER=paypal,paymongo

# ---- PayMongo (if BILLING_PROVIDER includes paymongo) ----
PAYMONGO_SECRET_KEY=sk_test_xxx
PAYMONGO_PUBLIC_KEY=pk_test_xxx
PAYMONGO_WEBHOOK_SECRET=whsec_xxx

# ---- PayPal (if BILLING_PROVIDER includes paypal) ----
PAYPAL_CLIENT_ID=YOUR_PAYPAL_SANDBOX_CLIENT_ID
PAYPAL_CLIENT_SECRET=YOUR_PAYPAL_SANDBOX_CLIENT_SECRET
PAYPAL_MODE=sandbox
PAYPAL_WEBHOOK_ID=YOUR_PAYPAL_SANDBOX_WEBHOOK_ID

# ---- OpenAI (only if core calls OpenAI routes) ----
OPENAI_API_KEY=YOUR_OPENAI_KEY
OPENAI_CHAT_MODEL=gpt-4.1-mini
OPENAI_EMBED_MODEL=text-embedding-3-large
OPENAI_TTS_MODEL=tts-1
OPENAI_TRANSCRIBE_MODEL=gpt-4o-mini-transcribe
OPENAI_MODERATION_MODEL=omni-moderation-latest
OPENAI_REALTIME_MODEL=gpt-realtime