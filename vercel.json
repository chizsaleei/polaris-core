{
  "version": 2,
  "installCommand": "npm ci",
  "buildCommand": "npm run build",
  "routes": [
    { "src": "/health", "dest": "/api/server" },
    { "src": "/version", "dest": "/api/server" },
    { "src": "/api/(.*)", "dest": "/api/server" }
  ]
}
